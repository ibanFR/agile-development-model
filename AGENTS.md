# AGENTS.md

## Agent skills

### Issue tracker

Issues live in GitHub Issues for `ibanFR/agile-development-model`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary, label strings equal to their names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root, ADRs in `adrs/`. See `docs/agents/domain.md`.
